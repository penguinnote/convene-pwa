node_modules
dist
dist-ssr
dev-dist
*.local
.env
.env.local
.DS_Store
.claude
.firebase
firebase-debug.log

# 인스턴스 작업 지침(로컬 유지, 공개 레포에서 감춤)
CLAUDE.md

# 디버그 로그(공백/번호 붙은 변형 포함)
firebase-debug*.log
firestore-debug*.log
.DS_Store
**/.DS_Store

# 임시 오피스 파일
~$*
Stats/

# Firebase 서비스 계정 키 (scripts/seed-dummy-data.js 용, 절대 커밋 금지)
serviceAccountKey.json
